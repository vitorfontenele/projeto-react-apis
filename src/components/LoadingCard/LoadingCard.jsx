import { 
    LoadingCardContainer,
    Loader
} from "./styled"

const LoadingCard = () => {
    return (
        <LoadingCardContainer>
            <Loader />
        </LoadingCardContainer>
    )
}

export default LoadingCard;